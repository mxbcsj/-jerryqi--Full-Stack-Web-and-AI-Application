- Git is a tool to track our coding
- Git will save the snapshot when we press commit

- Git commands learned in class:
  
git init

git config --global user.name

git config --global user.email

git branch bName

git switch bName

git status

git add .

git commit -m "add a file"

git push.origin main
